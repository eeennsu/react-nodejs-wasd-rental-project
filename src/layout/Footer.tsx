import type { FC } from 'react';

const Footer: FC = () => {
    
    // 크기를 보기위한 용도의 배경색. 추후 제거
    return (
        <div className='bg-blue-300'>
           Footer
        </div>
    );
};

export default Footer;