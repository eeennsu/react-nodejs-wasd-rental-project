import type { FC } from 'react';

const Header: FC = () => {

    // 크기를 보기위한 용도의 배경색. 추후 제거
    return (
        <div className='bg-red-300'>
           Header
        </div>
    );
};

export default Header;